sudo: required

services:
 - docker
script:
 - docker build -t jwhite1st/minecraftdocker -f Dockerfile
 - docker run --name jwhite1st/minecraftdocker -d minecraftdocker
 - docker logs minecraftdocker
